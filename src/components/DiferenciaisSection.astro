---
import { Icon } from 'astro-icon/components';

const items = [
  { icon: 'lucide:user-x', label: 'Sem perfil de condutor' },
  { icon: 'lucide:calendar-clock', label: 'Aceita veiculos antigos' },
  { icon: 'lucide:truck', label: 'Frotas empresariais' },
  { icon: 'lucide:smartphone', label: 'Apps (Uber/99)' },
  { icon: 'lucide:bus', label: 'Caminhoes e implementos' },
  { icon: 'lucide:caravan', label: 'Motor homes' },
  { icon: 'lucide:headset', label: 'Atendimento 24h humanizado' },
  { icon: 'lucide:message-circle', label: 'Canais digitais' },
];
---

<section id="diferenciais" class="relative py-28 text-primary-foreground overflow-hidden" aria-labelledby="diferenciais-heading">
  <!-- Gradient background -->
  <div
    class="absolute inset-0"
    style="background: linear-gradient(135deg, hsl(242 45% 16%) 0%, hsl(242 45% 22%) 50%, hsl(220 40% 18%) 100%);"
    aria-hidden="true"
  ></div>

  <div class="container mx-auto px-4 relative z-10">
    <div class="text-center mb-14" data-animate>
      <h2 id="diferenciais-heading" class="font-display text-3xl md:text-5xl font-extrabold mb-4 tracking-tight">Por que escolher a Nardini?</h2>
      <p class="text-primary-foreground/60 text-lg max-w-xl mx-auto">Diferenciais que fazem a diferenca na hora de proteger o que e seu.</p>
    </div>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-6" role="list">
      {items.map((item, i) => (
        <div
          class="flex flex-col items-center text-center p-6 rounded-2xl bg-white/5 border border-white/10 hover:bg-white/10 hover:shadow-lg hover:shadow-secondary/10 hover:-translate-y-0.5 transition-all duration-300"
          data-animate
          style={`transition-delay: ${i * 80}ms`}
          role="listitem"
        >
          <div class="w-12 h-12 rounded-xl bg-secondary/20 flex items-center justify-center mb-3" aria-hidden="true">
            <Icon name={item.icon} class="h-6 w-6 text-secondary" />
          </div>
          <span class="text-sm font-medium">{item.label}</span>
        </div>
      ))}
    </div>
  </div>
</section>
