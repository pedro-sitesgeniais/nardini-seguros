---
import { Icon } from 'astro-icon/components';

const items = [
  { icon: 'lucide:user', title: 'Pessoas Físicas', desc: 'Motoristas de todas as idades, do primeiro carro ao veículo de coleção. Sem restrição de perfil.' },
  { icon: 'lucide:briefcase', title: 'Pessoas Jurídicas', desc: 'Frotas empresariais de qualquer porte, com condições especiais e atendimento dedicado.' },
  { icon: 'lucide:users', title: 'Todos os Perfis', desc: 'Sem restrições de perfil, idade ou classe social. Proteção acessível para motoristas de app, caminhoneiros e mais.' },
];
---

<section class="py-24 bg-muted/50" aria-labelledby="paraquem-heading">
  <div class="container mx-auto px-4">
    <div class="text-center max-w-2xl mx-auto" data-animate>
      <h2 id="paraquem-heading" class="font-display text-3xl md:text-4xl font-extrabold text-primary mb-4 tracking-tight">Para Quem é a Nardini?</h2>
      <p class="text-muted-foreground text-lg mb-12 leading-relaxed">
        Nossos serviços são para <strong>todos</strong>. Não importa idade, classe social ou tipo de veículo.
      </p>
    </div>
    <div class="grid md:grid-cols-3 gap-8" data-animate style="transition-delay: 200ms" role="list">
      {items.map((item) => (
        <article class="flex items-start gap-4 p-6 rounded-2xl bg-background shadow-md hover:shadow-xl hover:-translate-y-1 border border-border/50 transition-all duration-300" role="listitem">
          <div class="w-12 h-12 rounded-xl bg-gradient-to-br from-secondary/15 to-secondary/5 flex items-center justify-center shrink-0" aria-hidden="true">
            <Icon name={item.icon} class="h-6 w-6 text-secondary" />
          </div>
          <div>
            <h3 class="font-display font-bold text-primary mb-1">{item.title}</h3>
            <p class="text-muted-foreground text-sm leading-relaxed">{item.desc}</p>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
